 SEVE: Phase 6 - UX & ATS Engine Synchronization

  ROLE: You are an elite Senior React/TypeScript Architect. Your current objective is to resolve a critical mismatch
  between the ATS scoring heuristics and the User Interface, and to recalibrate overly aggressive formatting warnings.

  CONTEXT:
  The ATS Engine (atsEvaluator.ts) is penalizing users for not having dates on their Projects. However, the UI
  (ProjectsForm.tsx) does not provide input fields for project dates. Furthermore, the ATS Engine is aggressively
  warning that bullets "wrap to 2+ lines" based on a strict 160 character limit, which is factually incorrect depending
   1. Locate the rendering block inside the projects.map loop.
   2. Below the "Project Name" and "Link" inputs, add a new layout row. You should probably put the "Link" input and the
      new "Date" input side-by-side (e.g., using grid grid-cols-2 gap-3).
   3. Add a new <input> field for the Date.
      - Value: project.date || ''
      - OnChange: handleChange(project.id, 'date', e.target.value)
      - Placeholder: e.g. 2023 - Present
      - Styling: Match the existing inputs (w-full bg-zinc-950 border border-zinc-800/80 rounded-lg px-3 py-2.5
        text-[13px] text-white...).
   4. Ensure the UI remains clean and doesn't break the existing Tailwind layouts.

  🟡  STEP 2: Recalibrate ATS Heuristics (atsEvaluator.ts)
  Target File: src/utils/atsEvaluator.ts
  We need to tone down stylistic checks so they act as coaching rather than "formatting errors".

   1. Fix the "Bullet Wraps" check:
      - Locate the scoreBulletQuality function.
      - Change TOO_LONG_CHARS from 160 to 180.
      - In the issues.push block for long bullets:
        - Change the type from 'warning' to 'suggestion'.
        - Change the category from 'formatting' to 'style'.
        - Change the issue text from ` Bullet wraps to 2+ lines (${charCount} chars)... ` to ` Bullet is quite long
          (${charCount} chars) and may be hard to scan quickly. `
        - Change severityScore from 6 to 2.

   2. Fix the "Weak Verb Start" check:
      - Locate the BULLET_CHECKS array.
      - Find the check named 'Weak verb start'.
      - Change category: 'style' (leave it as is).
      - Change type: 'warning' to type: 'suggestion'.

   3. Verify Date logic:
      - Ensure that scoreDateConsistency handles the newly exposed single date string from the project object without
        throwing errors, treating it the same way it treats certification or award dates. (It should already do this via
        the allDates extraction logic, but verify it doesn't crash).

  VERIFICATION:
  After completing these steps, run npm run lint and npm run build to ensure no types or styles were broken. Do not
  modify the core scoring algorithm totals, just the severity types and UI bindings.