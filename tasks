
• Here is a strong implementation brief you can give to DeepSeek Flash v4.

  Implementation Guide For Seve Audit Fixes

  You are implementing fixes in C:\Users\pc\Desktop\seve. Do not refactor unrelated code. Preserve existing UX unless
  the fix requires text or behavior changes. The repo may have user edits already, so do not revert files blindly.

  Primary goal: make the app truthful, buildable, safer around Supabase, and clearer around cloud/local storage.

  Phase 1: Fix Critical Truth/Privacy Drift

  1. Update privacy-facing copy so it matches the current product.
  2. Files:
      - README.md
      - src/pages/PrivacyPage.tsx
      - likely src/pages/LandingPage.tsx

  3. Current false claims:
      - “no tracking”
      - “zero third-party servers”
      - “we never run databases”
      - “your data never leaves your browser”

  4. Replace with accurate language:
      - Resume editing is local-first.
      - Anonymous page-view counts are recorded through Supabase.
      - Google sign-in is optional/required for cloud sync and PDF export depending on intended product behavior.
      - Signed-in users can sync resume data to Supabase.
      - State exactly which data is stored locally and which data may be sent to Supabase.

  5. Do not overpromise “absolute privacy” if cloud sync exists.

  Phase 2: Prevent Supabase Env Blank Screen

  1. Fix src/lib/supabase.ts.
  2. Current behavior throws if VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY is missing.
  3. Required behavior:
      - App should still load in local-only mode without Supabase env vars.
      - Supabase-dependent features should show disabled/error/local-only state instead of crashing.

  4. Likely approach:
      - Export supabase as nullable, or export helper flags like isSupabaseConfigured.
      - Update consumers:
          - AuthContext.tsx
          - ResumeContext.tsx
          - usePageViews.ts

      - If Supabase is missing:
          - Auth provider returns user: null, loading: false, and sign-in gives a friendly error.
          - Resume sync stays local.
          - Page views silently disabled.

  Phase 3: Fix Supabase Analytics RLS

  1. Update both schema files if duplicated:
      - supabase/migrations.sql
      - supabase-schema.sql

  2. Current issue:
      - page_views allows public SELECT of raw visitor rows.
      - Client uses upsert, but there is no UPDATE policy, so conflict upserts may fail silently.

  3. Safer target:
      - Anonymous clients can insert page views.
      - Anonymous clients cannot select raw page_views.
      - Aggregate counts are available only through RPC functions.

  4. Options:
      - Remove "Anyone can select page views" policy.
      - Keep count_distinct_visitors RPC.
      - Prefer insert-ignore client behavior instead of upsert update.

  5. If keeping upsert, add a narrow update policy only for same visitor_id/path/date, but insert-ignore is cleaner.

  Phase 4: Fix Cloud Save Semantics

  1. Files:
      - src/context/ResumeContext.tsx
      - src/layouts/EditorLayout.tsx

  2. Current mismatch:
      - Comments say cloud auto-save is debounced.
      - Actual behavior only writes localStorage and marks changes unsaved.
      - Ctrl+S is intercepted but does not save.

  3. Choose one product behavior and make code/copy match. Recommended:
      - Local save is automatic.
      - Cloud save is manual.
      - Ctrl+S should call saveChangesToCloud() if user is signed in and has unsaved cloud changes.
      - Status text should say “Saved locally”, “Unsaved cloud changes”, “Cloud saved”, etc.

  4. Remove misleading comments about debounced cloud sync unless you actually implement it.

  Phase 5: Make Build Pass

  1. Run:
      - npx.cmd tsc -b --pretty false --noEmit

  2. Known current blocker:
      - src/layouts/EditorLayout.tsx: unused useCallback import.

  3. Fix all TypeScript build errors without weakening tsconfig.

  Phase 6: Fix Lint Scope

  1. File:
      - eslint.config.js

  2. Current issue:
      - npm run lint scans reactive-resume-src, which appears to be imported/reference source, not the active app.

  3. Add global ignores for:
      - dist
      - node_modules
      - reactive-resume-src

  4. Then run:
      - npm.cmd run lint

  5. Fix active src lint issues pragmatically. Do not rewrite the whole app.

  Phase 7: Active Lint Fixes
  Known categories:

  1. Ref access during render:
      - src/context/ResumeContext.tsx
      - src/components/AtsChecker.tsx
      - src/hooks/useUndoRedo.ts

  2. Empty catch blocks:
      - replace with short comments or no-op helper if appropriate.

  3. any usage:
      - replace with narrow types where simple.
      - Do not create huge type systems just to satisfy lint.

  4. React refresh warnings:
      - split exported constants/helpers from component files only if needed.
      - If these are low-value warnings, consider config tuning, but prefer clean structure for active app files.

  Phase 8: Backup/Export Product Fix

  1. File:
      - src/layouts/EditorLayout.tsx

  2. Current “Export” only exports resumeData, not:
      - resume title
      - selected template
      - style prefs
      - section order
      - multiple resume versions
      - selected resume id

  3. Recommended:
      - Rename current action to “Export Current Resume JSON”, or
      - Export full app state.

  4. Best product behavior:
      - Add full backup export/import for AppState.
      - Keep single-resume import supported for backward compatibility.

  Phase 9: Database ID Robustness

  1. Current schema uses global id TEXT PRIMARY KEY.
  2. All users can begin with the same default resume ID, causing cross-user collisions.
  3. Recommended schema direction:
      - Use composite uniqueness/primary key on (user_id, id), or
      - Generate a fresh UUID before first cloud insert for default local resumes.

  4. If changing schema is risky, at least make the client convert the fixed default ID to a real UUID before first
     cloud save.

  Verification Required
  After implementation, run:

  npm.cmd test
  npx.cmd tsc -b --pretty false --noEmit
  npm.cmd run lint
  npm.cmd audit --omit=dev --audit-level=moderate
  npm.cmd run build

  Expected final state:

  - Tests pass.
  - TypeScript passes.
  - Lint passes or only has explicitly documented acceptable warnings.
  - Build passes.
  - App can start without Supabase env vars in local-only mode.
  - Privacy copy accurately describes Supabase, Google sign-in, cloud sync, and analytics.

  Do Not Do

  - Do not delete reactive-resume-src unless explicitly asked.
  - Do not commit .env.
  - Do not weaken TypeScript strictness to hide errors.
  - Do not remove RLS from Supabase tables.
  - Do not claim “no tracking” while page-view logging exists.
  - Do not silently change product behavior around sign-in/PDF export without updating UI copy.
